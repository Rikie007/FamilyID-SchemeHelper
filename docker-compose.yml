services:
  mongo:
    image: mongo:7
    container_name: gj-family-id-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: gj_family_id
    volumes:
      - gj_mongo_data:/data/db

volumes:
  gj_mongo_data:
